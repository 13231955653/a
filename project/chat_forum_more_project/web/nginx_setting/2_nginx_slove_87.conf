server {
        listen        87;
        server_name  you.com;
        root   "/usr/local/project1_static_resource_slove_2";

        location ~ .*\.(gif|jpg|jpeg|png|bmp|swf)$
        {
            expires 99d;
            access_log off;
        }

        location ~ \.(js|css)$
        {
            expires 99d;
        }

        location / {
            index index.html;
        }
}
