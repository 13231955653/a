server {
        listen        86;
        server_name  you.com;
        root   "/usr/local/project1_static_resource_slove_1";

        location ~ .*\.(gif|jpg|jpeg|png|bmp|swf)$
        {
            expires 999d;
            access_log off;
        }

        location ~ \.(js|css)$
        {
            expires 999d;
        }

        location / {
            index index.html;
        }
}
